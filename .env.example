# RXShare Configuration

# Server port
PORT=3000

# JWT secret — CHANGE THIS to a random string in production
JWT_SECRET=change-this-secret-key-in-production

# Database type: sqlite (default) or mysql
DB_TYPE=sqlite

# MySQL/MariaDB connection (only if DB_TYPE=mysql)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=rxshare
# DB_PASSWORD=your-password
# DB_NAME=rxshare

# Storage type: local (default) or s3
STORAGE_TYPE=local

# S3-compatible storage (only if STORAGE_TYPE=s3)
# Works with AWS S3, Cloudflare R2, MinIO, etc.
# S3_BUCKET=your-bucket
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_ENDPOINT=https://s3.amazonaws.com
# For Cloudflare R2: S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com

# Encryption at rest (optional — if not set, files are stored unencrypted)
# Must be a 64-character hex string (32 bytes for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=
