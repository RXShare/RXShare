services:
  rxshare:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - rxshare-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  rxshare-data:
